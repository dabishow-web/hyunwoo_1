rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Broadly allow all operations for testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
